# Smart Contracts (planned)

Placeholder for the on-chain attestation contracts.

**Not implemented.** When implemented, this will hold a single EVM testnet
contract (e.g. Solidity) that records `AttestationRecord` hashes. No raw
media or biometric data will ever be stored on-chain — only content/metadata
hashes.
