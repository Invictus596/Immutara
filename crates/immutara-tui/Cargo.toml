[package]
name = "immutara-tui"
version = "0.1.0"
edition = "2024"
description = "Ratatui-based terminal UI for Immutara pipeline visualization"

[dependencies]
immutara-core = { path = "../immutara-core" }
ratatui = "0.29"
crossterm = "0.28"
tokio = { version = "1", features = ["sync", "time"] }
